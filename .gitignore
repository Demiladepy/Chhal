# venv / python
.venv/
__pycache__/
*.pyc
.pytest_cache/
.ipynb_checkpoints/

# results are regenerated by scripts/run_loop.py; keep the dir, ignore heavy artifacts
results/*.csv
!results/.gitkeep
# ...except the small ones the README tables cite and the dashboard replays, so a judge
# can check every number (and run the dashboard) without a full re-run first
!results/curve.csv
!results/per_vector_recall.csv
!results/sample_attacks.csv
!results/fidelity_per_vector.csv
!results/fidelity_mimicry_ks.csv
!results/generalisation_leave_one_out.csv

# os / editor
.DS_Store
Thumbs.db
.vscode/
.idea/

# internal strategy notes, not part of the submission
RESEARCH.md
